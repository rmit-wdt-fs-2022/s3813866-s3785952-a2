@using Assignment2.Utility
@model Account
@{
    ViewData["Title"] = "Deposit";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h3 class="text-center ">
                Current Account
            </h3>
            <div class="row">
                <div class="col-md-12">
                    <table class="table table-hover table-bordered">
                        <thead>
                        <tr>
                            <th>
                                Account Number
                            </th>
                            <th>
                                Account Type
                            </th>
                            <th>
                                Current Balance
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        <tr>
                            <td>
                                @Model.AccountNumber
                            </td>
                            <td>
                                @{
                                    string? accountType = null;
                                    char savings = 'S';
                                    char checking = 'C';
                                    if (Model.AccountType.Equals(savings))
                                    {
                                        accountType = "Savings";
                                    }
                                    else if (Model.AccountType.Equals(checking))
                                    {
                                        accountType = "Checking";
                                    }
                                }
                                <a>@accountType</a>
                            </td>
                            <td>
                                @Model.Transactions.CalculateAccountBalance().ToString("C")
                            </td>
                        </tr>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">

                    <form asp-action="Transfer">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="form-group">
                                    <label for="AccountNumber" class="control-label">Enter Target Account Number :</label>
                                    <input id="AccountNumber" name="AccountNumber" class="form-control" value="@ViewBag.AccountNumber"
                                           data-val="true" data-val-number="The field Account must be a number."
                                           data-val-required="The Account field is required."/>
                                    @Html.ValidationMessage("AccountNumber", new {@class = "text-danger"})
                                    <label for="Comment" class="control-label"> Enter A Comment</label>
                                    <input id="Comment" name="Comment" class="form-control" placeholder="Add A Comment"/>

                                    <label for="Amount" class="control-label">Enter A Amount To Be Transfered : </label>
                                    <input id="Amount" name="Amount" class="form-control" value="@ViewBag.Amount?.ToString("0.00")"
                                           data-val="true" data-val-number="The field Amount must be a number."
                                           data-val-required="The Amount field is required."/>
                                    @Html.ValidationMessage("Amount", new {@class = "text-danger"})
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 button-deposit-right">
                                <div class=" col-md-6">
                                    <div class="form-group">
                                        <input type="submit" value="Transfer" class="btn btn-primary"/>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6 button-deposit-left">
                                <div class=" col-md-6">
                                    <div class="form-group">
                                        <a asp-action="Index" class="btn btn-block btn-danger">
                                            Cancel
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}